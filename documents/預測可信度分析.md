# 預測可信度分析

## 🎯 什麼是預測可信度？

**預測可信度（Prediction Confidence/Uncertainty）** 是指模型對其預測結果的確定程度。

### 為什麼重要？

```
預測值: 開盤漲幅 = +8.5%

但是：
- 可信度高（90%）→ 可以信賴這個預測
- 可信度低（30%）→ 這個預測不太可靠
```

**實際應用**：
- 可信度高的預測 → 可以採取行動
- 可信度低的預測 → 謹慎觀望

---

## 📊 回歸模型的可信度指標

### 1. 預測區間（Prediction Interval）

**定義**：預測值的可能範圍

```
預測: 開盤漲幅 = +8.5%
95% 預測區間: [+3.2%, +13.8%]

解讀:
- 有 95% 的機率，真實值會落在 +3.2% 到 +13.8% 之間
- 區間越窄 → 可信度越高
- 區間越寬 → 可信度越低
```

**計算方法**：
```python
from sklearn.ensemble import GradientBoostingRegressor
import numpy as np

# 使用 Quantile Regression 估計預測區間
model_lower = GradientBoostingRegressor(loss='quantile', alpha=0.025)  # 2.5%
model_upper = GradientBoostingRegressor(loss='quantile', alpha=0.975)  # 97.5%

model_lower.fit(X_train, y_train)
model_upper.fit(X_train, y_train)

# 預測
y_pred = model.predict(X_test)
y_lower = model_lower.predict(X_test)
y_upper = model_upper.predict(X_test)

# 預測區間
prediction_interval = y_upper - y_lower
```

### 2. 標準誤差（Standard Error）

**定義**：預測的平均誤差

```python
# 計算標準誤差
from sklearn.metrics import mean_squared_error

y_pred = model.predict(X_test)
mse = mean_squared_error(y_test, y_pred)
std_error = np.sqrt(mse)

print(f"標準誤差: {std_error:.2f}%")
# 例如: 標準誤差: 4.75%

# 可信度解讀
# 預測值 ± 2×標準誤差 約涵蓋 95% 的情況
confidence_interval = 2 * std_error
print(f"95% 信賴區間: ±{confidence_interval:.2f}%")
```

### 3. 模型不確定性（Model Uncertainty）

**方法 1: 使用多個模型（Ensemble）**

```python
from sklearn.ensemble import RandomForestRegressor, GradientBoostingRegressor
from sklearn.linear_model import Ridge
import xgboost as xgb

# 訓練多個不同的模型
models = {
    'XGBoost': xgb.XGBRegressor(),
    'RandomForest': RandomForestRegressor(),
    'GradientBoosting': GradientBoostingRegressor(),
    'Ridge': Ridge()
}

predictions = []
for name, model in models.items():
    model.fit(X_train, y_train)
    pred = model.predict(X_test)
    predictions.append(pred)

# 計算預測的平均值和標準差
predictions = np.array(predictions)
mean_pred = predictions.mean(axis=0)
std_pred = predictions.std(axis=0)

# 標準差越大 → 模型之間分歧越大 → 可信度越低
print(f"預測值: {mean_pred[0]:.2f}%")
print(f"模型分歧度: {std_pred[0]:.2f}%")
```

**方法 2: Bootstrap 重採樣**

```python
from sklearn.utils import resample

# 訓練多個模型（使用不同的訓練子集）
n_iterations = 100
predictions = []

for i in range(n_iterations):
    # 重採樣訓練資料
    X_boot, y_boot = resample(X_train, y_train, random_state=i)
    
    # 訓練模型
    model = xgb.XGBRegressor(random_state=i)
    model.fit(X_boot, y_boot)
    
    # 預測
    pred = model.predict(X_test)
    predictions.append(pred)

# 計算統計量
predictions = np.array(predictions)
mean_pred = predictions.mean(axis=0)
std_pred = predictions.std(axis=0)
lower_bound = np.percentile(predictions, 2.5, axis=0)
upper_bound = np.percentile(predictions, 97.5, axis=0)

print(f"預測值: {mean_pred[0]:.2f}%")
print(f"95% 預測區間: [{lower_bound[0]:.2f}%, {upper_bound[0]:.2f}%]")
print(f"區間寬度: {upper_bound[0] - lower_bound[0]:.2f}%")
```

### 4. 特徵相似度（Feature Similarity）

**概念**：新資料與訓練資料的相似程度

```python
from sklearn.neighbors import NearestNeighbors

# 找出訓練集中最相似的樣本
nn = NearestNeighbors(n_neighbors=5)
nn.fit(X_train)

# 計算新樣本到最近鄰居的距離
distances, indices = nn.kneighbors(X_test)

# 平均距離越小 → 越相似 → 可信度越高
avg_distance = distances.mean(axis=1)

# 標準化距離（0-1）
max_distance = distances.max()
similarity_score = 1 - (avg_distance / max_distance)

print(f"相似度分數: {similarity_score[0]:.2f}")
# 0.9 → 非常相似，可信度高
# 0.3 → 不太相似，可信度低
```

---

## 🎯 XGBoost 的可信度估計

### 方法 1: 使用樹的預測分布

```python
import xgboost as xgb

# 訓練模型
model = xgb.XGBRegressor(n_estimators=100, random_state=42)
model.fit(X_train, y_train)

# 獲取每棵樹的預測
booster = model.get_booster()
# 注意：XGBoost 不直接提供每棵樹的預測，需要自己實作

# 替代方案：使用 predict 的 ntree_limit 參數
predictions = []
for n_trees in range(10, 101, 10):
    pred = model.predict(X_test, ntree_limit=n_trees)
    predictions.append(pred)

predictions = np.array(predictions)
std_pred = predictions.std(axis=0)

print(f"預測不確定性: {std_pred[0]:.2f}%")
```

### 方法 2: 使用 Quantile Regression

```python
# 訓練三個模型：下界、中位數、上界
model_lower = xgb.XGBRegressor(
    objective='reg:quantileerror',
    quantile_alpha=0.05,  # 5% 分位數
    n_estimators=100
)

model_median = xgb.XGBRegressor(
    objective='reg:quantileerror',
    quantile_alpha=0.5,   # 50% 分位數（中位數）
    n_estimators=100
)

model_upper = xgb.XGBRegressor(
    objective='reg:quantileerror',
    quantile_alpha=0.95,  # 95% 分位數
    n_estimators=100
)

# 訓練
model_lower.fit(X_train, y_train)
model_median.fit(X_train, y_train)
model_upper.fit(X_train, y_train)

# 預測
y_lower = model_lower.predict(X_test)
y_median = model_median.predict(X_test)
y_upper = model_upper.predict(X_test)

# 90% 預測區間
print(f"預測值: {y_median[0]:.2f}%")
print(f"90% 預測區間: [{y_lower[0]:.2f}%, {y_upper[0]:.2f}%]")
print(f"區間寬度: {y_upper[0] - y_lower[0]:.2f}%")
```

---

## 💡 實用的可信度指標

### 綜合可信度分數

```python
def calculate_confidence_score(
    prediction,
    prediction_interval_width,
    similarity_score,
    model_std
):
    """
    計算綜合可信度分數（0-1）
    
    參數:
    - prediction: 預測值
    - prediction_interval_width: 預測區間寬度
    - similarity_score: 與訓練資料的相似度
    - model_std: 模型預測的標準差
    """
    
    # 1. 預測區間分數（區間越窄越好）
    # 假設區間寬度 < 5% 為高可信度
    interval_score = max(0, 1 - prediction_interval_width / 10)
    
    # 2. 相似度分數（已經是 0-1）
    # similarity_score 已經標準化
    
    # 3. 模型一致性分數（標準差越小越好）
    # 假設標準差 < 2% 為高可信度
    consistency_score = max(0, 1 - model_std / 4)
    
    # 綜合分數（加權平均）
    confidence = (
        0.4 * interval_score +
        0.3 * similarity_score +
        0.3 * consistency_score
    )
    
    return confidence

# 使用範例
confidence = calculate_confidence_score(
    prediction=8.5,
    prediction_interval_width=6.2,  # [+5.4%, +11.6%]
    similarity_score=0.75,
    model_std=1.8
)

print(f"可信度分數: {confidence:.2f}")
# 0.8-1.0: 高可信度
# 0.5-0.8: 中等可信度
# 0.0-0.5: 低可信度
```

---

## 🔧 實作：為你的專案增加可信度

### 步驟 1: 修改預測程式

```python
def predict_with_confidence(model, X_new, X_train, y_train):
    """
    預測並計算可信度
    
    返回:
    - prediction: 預測值
    - confidence: 可信度分數（0-1）
    - interval: 預測區間 [lower, upper]
    """
    
    # 1. 基本預測
    prediction = model.predict(X_new)[0]
    
    # 2. 使用 Bootstrap 估計預測區間
    n_iterations = 50
    predictions = []
    
    for i in range(n_iterations):
        # 重採樣
        indices = np.random.choice(len(X_train), len(X_train), replace=True)
        X_boot = X_train[indices]
        y_boot = y_train[indices]
        
        # 訓練
        boot_model = xgb.XGBRegressor(
            n_estimators=100,
            max_depth=5,
            learning_rate=0.1,
            random_state=i
        )
        boot_model.fit(X_boot, y_boot)
        
        # 預測
        pred = boot_model.predict(X_new)[0]
        predictions.append(pred)
    
    predictions = np.array(predictions)
    
    # 3. 計算統計量
    mean_pred = predictions.mean()
    std_pred = predictions.std()
    lower = np.percentile(predictions, 2.5)
    upper = np.percentile(predictions, 97.5)
    interval_width = upper - lower
    
    # 4. 計算相似度
    from sklearn.neighbors import NearestNeighbors
    nn = NearestNeighbors(n_neighbors=5)
    nn.fit(X_train)
    distances, _ = nn.kneighbors(X_new)
    avg_distance = distances.mean()
    max_distance = np.linalg.norm(X_train.max(axis=0) - X_train.min(axis=0))
    similarity_score = 1 - (avg_distance / max_distance)
    
    # 5. 計算可信度
    confidence = calculate_confidence_score(
        prediction=mean_pred,
        prediction_interval_width=interval_width,
        similarity_score=similarity_score,
        model_std=std_pred
    )
    
    return {
        'prediction': prediction,
        'mean_prediction': mean_pred,
        'confidence': confidence,
        'interval': (lower, upper),
        'interval_width': interval_width,
        'std': std_pred,
        'similarity': similarity_score
    }
```

### 步驟 2: 使用範例

```python
# 預測
result = predict_with_confidence(model, X_new, X_train, y_train)

print(f"預測值: {result['prediction']:.2f}%")
print(f"可信度: {result['confidence']:.2f}")
print(f"95% 預測區間: [{result['interval'][0]:.2f}%, {result['interval'][1]:.2f}%]")
print(f"相似度: {result['similarity']:.2f}")

# 根據可信度決策
if result['confidence'] > 0.7:
    print("✅ 高可信度，可以採取行動")
elif result['confidence'] > 0.4:
    print("⚠️ 中等可信度，謹慎評估")
else:
    print("❌ 低可信度，建議觀望")
```

---

## 📊 可信度的視覺化

```python
import matplotlib.pyplot as plt

def plot_prediction_with_confidence(results):
    """視覺化預測結果和可信度"""
    
    fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(14, 5))
    
    # 圖1: 預測區間
    stocks = [r['stock'] for r in results]
    predictions = [r['prediction'] for r in results]
    lowers = [r['interval'][0] for r in results]
    uppers = [r['interval'][1] for r in results]
    confidences = [r['confidence'] for r in results]
    
    # 顏色根據可信度
    colors = ['green' if c > 0.7 else 'orange' if c > 0.4 else 'red' 
              for c in confidences]
    
    ax1.scatter(range(len(stocks)), predictions, c=colors, s=100, zorder=3)
    for i in range(len(stocks)):
        ax1.plot([i, i], [lowers[i], uppers[i]], c=colors[i], linewidth=2)
    
    ax1.set_xticks(range(len(stocks)))
    ax1.set_xticklabels(stocks, rotation=45)
    ax1.set_ylabel('預測漲幅 (%)')
    ax1.set_title('預測值與預測區間')
    ax1.grid(True, alpha=0.3)
    ax1.axhline(y=0, color='black', linestyle='--', alpha=0.5)
    
    # 圖2: 可信度分數
    ax2.barh(range(len(stocks)), confidences, color=colors)
    ax2.set_yticks(range(len(stocks)))
    ax2.set_yticklabels(stocks)
    ax2.set_xlabel('可信度分數')
    ax2.set_title('預測可信度')
    ax2.set_xlim(0, 1)
    ax2.axvline(x=0.7, color='green', linestyle='--', alpha=0.5, label='高可信度')
    ax2.axvline(x=0.4, color='orange', linestyle='--', alpha=0.5, label='中等可信度')
    ax2.legend()
    
    plt.tight_layout()
    plt.savefig('prediction_confidence.png', dpi=300, bbox_inches='tight')
    plt.show()
```

---

## ⚠️ 注意事項

### 1. 可信度 ≠ 準確度

```
可信度高 ≠ 預測一定準確
可信度低 ≠ 預測一定錯誤

可信度只是告訴你：
- 模型對這個預測有多確定
- 這個預測的不確定性有多大
```

### 2. 資料量影響可信度

```
資料量少（31 筆）:
- 所有預測的可信度都會偏低
- 預測區間會很寬

資料量多（1000+ 筆）:
- 可信度會更可靠
- 預測區間會更窄
```

### 3. 外推問題

```
如果新資料與訓練資料差異很大：
- 相似度分數會很低
- 可信度會很低
- 預測可能不可靠

範例：
訓練資料: 產業 10-20（科技、工業）
新資料: 產業 44（消費品）
→ 相似度低，可信度低
```

---

## 🎯 總結

### 回答你的問題

**是的，預測結果有「可信度」！**

### 主要方法

1. **預測區間**：估計預測值的可能範圍
2. **標準誤差**：預測的平均誤差
3. **模型不確定性**：使用多個模型或 Bootstrap
4. **特徵相似度**：新資料與訓練資料的相似程度

### 實用建議

1. **使用 Bootstrap** 估計預測區間（最實用）
2. **計算相似度** 判斷是否外推
3. **綜合多個指標** 得出可信度分數
4. **根據可信度決策**：
   - 高可信度（> 0.7）→ 可以採取行動
   - 中等可信度（0.4-0.7）→ 謹慎評估
   - 低可信度（< 0.4）→ 建議觀望

### 你的專案

由於資料量少（31 筆），目前所有預測的可信度都會偏低。建議：
1. 先收集更多資料
2. 達到 100+ 筆後再實作可信度估計
3. 使用 Bootstrap 方法最簡單有效

---

**記住**：可信度是風險管理的重要工具，不要只看預測值，也要看可信度！
