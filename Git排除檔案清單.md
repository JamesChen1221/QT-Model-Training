# Git 排除檔案清單

## ✅ 已排除的檔案類型

以下檔案類型已在 `.gitignore` 中設定，**不會**被上傳到 GitHub：

### 📊 資料檔案

#### Excel 檔案
- `*.xlsx` - 所有 Excel 2007+ 檔案
- `*.xls` - 所有舊版 Excel 檔案
- `data/*.xlsx` - data 目錄下的所有 Excel 檔案
- `data/*.xls` - data 目錄下的所有舊版 Excel 檔案

**具體檔案**：
- ❌ `data/QT Training Data.xlsx` - 訓練資料
- ❌ `data/Stock TBP.xlsx` - 預測資料

#### CSV 檔案
- `*.csv` - 所有 CSV 檔案
- `data/new_predictions.csv` - 預測結果

**具體檔案**：
- ❌ `data/new_predictions.csv` - 預測結果（自動生成）

### 🤖 模型檔案

- `models/*.pkl` - 所有 pickle 模型檔案
- `models/*.h5` - 所有 HDF5 模型檔案
- `models/*.keras` - 所有 Keras 模型檔案
- `models/*.png` - 所有訓練結果圖表

**具體檔案**：
- ❌ `models/qt_model_開盤_pct.pkl`
- ❌ `models/qt_model_10分鐘低價_pct.pkl`
- ❌ `models/qt_model_1.5小時高價_pct.pkl`
- ❌ `models/qt_model_最高價前的最低價_pct.pkl`
- ❌ `models/training_results_*.png`

### 🐍 Python 相關

- `__pycache__/` - Python 快取目錄
- `*.py[cod]` - Python 編譯檔案
- `*$py.class` - Python class 檔案
- `*.so` - 共享物件檔案
- `.Python` - Python 設定
- `*.egg-info/` - Python 套件資訊
- `dist/` - 發布目錄
- `build/` - 建置目錄

### 🌐 虛擬環境

- `env/` - 虛擬環境目錄
- `venv/` - 虛擬環境目錄
- `.venv/` - 虛擬環境目錄

### 📓 Jupyter Notebook

- `.ipynb_checkpoints/` - Jupyter 檢查點
- `*.ipynb` - Jupyter Notebook 檔案

### 📝 日誌檔案

- `*.log` - 所有日誌檔案

**具體檔案**：
- ❌ `train_output.txt` - 訓練輸出日誌

### 💻 IDE 設定

- `.vscode/` - VS Code 設定
- `.idea/` - PyCharm 設定
- `*.swp` - Vim swap 檔案
- `*.swo` - Vim swap 檔案

### 🖥️ 系統檔案

- `.DS_Store` - macOS 系統檔案
- `Thumbs.db` - Windows 縮圖快取
- `desktop.ini` - Windows 桌面設定

### 📦 臨時檔案

- `.tmp.driveupload/` - 臨時上傳目錄
- `*.tmp` - 臨時檔案
- `*.bak` - 備份檔案

## ✅ 會被上傳的檔案

### 程式碼
- ✅ `train_qt_xgboost.py` - 訓練程式
- ✅ `predict_new_stocks.py` - 預測程式

### 工具
- ✅ `快速訓練.bat` - 快速訓練批次檔
- ✅ `快速預測.bat` - 快速預測批次檔

### 文檔
- ✅ `README.md` - 專案說明
- ✅ `data/README.md` - 資料格式說明
- ✅ `GITHUB_UPLOAD.md` - GitHub 上傳指南
- ✅ `上傳前檢查清單.md` - 檢查清單
- ✅ `Git排除檔案清單.md` - 本文件
- ✅ `專案結構.md` - 專案結構說明
- ✅ `訓練概念說明.md` - 訓練概念說明

### 配置
- ✅ `requirements.txt` - Python 依賴套件
- ✅ `.gitignore` - Git 忽略規則

## 🔍 驗證方法

### 檢查哪些檔案會被上傳

```bash
# 列出所有被 Git 追蹤的檔案
git ls-files

# 檢查是否有 Excel 檔案
git ls-files | grep -i xlsx
# 應該沒有輸出

# 檢查是否有 CSV 檔案
git ls-files | grep -i csv
# 應該沒有輸出

# 檢查是否有模型檔案
git ls-files | grep -i pkl
# 應該沒有輸出
```

### 檢查本地檔案是否存在

```bash
# 檢查 Excel 檔案（應該存在）
ls data/*.xlsx

# 檢查 CSV 檔案（如果有執行過預測）
ls data/*.csv

# 檢查模型檔案（如果有訓練過）
ls models/*.pkl
```

## ⚠️ 重要提醒

1. **資料隱私**
   - Excel 檔案包含你的訓練資料，不會被上傳
   - CSV 預測結果也不會被上傳
   - 這些檔案只存在於你的本地電腦

2. **模型檔案**
   - 模型檔案很大（通常 > 100KB）
   - 不適合放在 Git 倉庫中
   - 其他人需要自行訓練模型

3. **其他人使用此專案**
   - 需要準備自己的訓練資料
   - 需要自行訓練模型
   - 參考 `data/README.md` 了解資料格式

## 📋 完整的 .gitignore 內容

```gitignore
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/

# 虛擬環境
env/
venv/
.venv/

# 模型檔案（自動生成）
models/*.pkl
models/*.h5
models/*.keras
models/*.png

# 資料檔案
*.csv
*.xlsx
*.xls
data/new_predictions.csv
data/*.xlsx
data/*.xls

# Jupyter Notebook
.ipynb_checkpoints/
*.ipynb

# 日誌
*.log

# IDE
.vscode/
.idea/
*.swp
*.swo

# 系統檔案
.DS_Store
Thumbs.db
desktop.ini

# 臨時檔案
.tmp.driveupload/
*.tmp
*.bak
```

---

**總結**：所有敏感資料（Excel、CSV、模型）都已被排除，可以安全上傳到 GitHub！✅
